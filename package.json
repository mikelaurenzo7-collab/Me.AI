{
  "name": "me-ai",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAI-powered native iOS and CarPlay voice agent workspace.",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm --workspace backend run dev",
    "build": "npm --workspace backend run build",
    "typecheck": "npm --workspace backend run typecheck"
  }
}
